import React from 'react'
import SidePanel from '../SidePanel';

const Modal = (props) => {
    return (
        <div id="side-panel--modal">
            <SidePanel />
        </div>
    )
}

export default Modal